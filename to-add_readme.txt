dynamic type line that shows while entering code
array inside an array for better C function.
rename c and ce to better fit the functional desciption (c backs up 1 step) Ce resets the machine.
Use the reset function at the start to save memory instead of double defining some parts.


Add negate function
add calculate percentages.
add calculate root.
add calculate quadrant.

set jss in seprate file
set style in a seperate file
make the math machine modulair so it's easier to scrap code and integrate seperate functions into other code
